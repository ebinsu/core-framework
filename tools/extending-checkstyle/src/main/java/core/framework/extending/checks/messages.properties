method.param.disallow.type=method parameter type is disallow.

domain.layer.pkgs.disallow=The domain layer cannot reference the application layer, user interface layer, or infrastructure layer.
domain.persistence.pkgs.disallow=There can only use the repository to manipulate the aggregation root.
domain.entities.pkgs.disallow=There can only refer to aggregates, entities, and value objects within the same context.
domain.multiple.agg.disallow=There can be only one aggregation root in a context.