method.param.disallow.type=方法参数类型不被允许。

domain.layer.pkgs.disallow=领域层不能引用应用层、用户接口层、基础设施层。
domain.persistence.pkgs.disallow=只能使用存储库（Repository）来操作聚合根。
domain.entities.pkgs.disallow=只能在同一上下文中引用聚合、实体和值对象。
domain.multiple.agg.disallow=一个上下文中只能有一个聚合根。