spring:
  datasource:
    url: jdbc:mysql://172.24.110.87:30001/mall?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ebinsu12345!

core:
  named-query:
    hot-reload: true
  hibernate:
    packages-to-scan: app.*
    hbm2ddl: "create"
    show-sql: true
  security:
    login-request:
      authentication-type: "USERNAME_PASSWORD"
      url: "/security/login"
    logout-request:
      url: "/security/logout"
    exclude-patterns:
      - "/*"
    security-type: INTERCEPTOR